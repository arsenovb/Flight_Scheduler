@model IEnumerable<Flight_Scheduler.Models.Flight>

@{
    ViewData["Title"] = "Home Page";
}
<div class="container text-center">
    <h1>Добре дошли във Flight Scheduler!</h1>

</div>
<div class="text-center">
    <img src="~/images/flight-image.webp.jpg" alt="Flight Image" class="img-fluid mb-4" style="max-height: 400px;" />

    <p class="lead">
        Flight Scheduler е уеб платформа, създадена да улесни управлението и планирането на полетите във всяко летище.
        С няколко лесни стъпки можете лесно да създавате, редактирате и следите полети,
        както и да разпределяте наличен екипаж и самолети според капацитета и изискванията на всяка машина.
    </p>
</div>

<h2 class="mt-5">Upcoming Flights</h2>

<table class="table table-bordered table-striped mt-3">
    <thead class="thead-dark">
        <tr>
            <th>Origin</th>
            <th>Destination</th>
            <th>Departure</th>
            <th>Arrival</th>
            <th>Gate</th>
            <th>Airline</th>
            <th>Aircraft</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var flight in Model)
        {
            <tr>
                <td>@flight.Origin.Name</td>
                <td>@flight.Destination.Name</td>
                <td>@flight.DepartureTime.ToString("g")</td>
                <td>@flight.ArrivalTime.ToString("g")</td>
                <td>@flight.Gate</td>
                <td>@flight.Airlines?.Name</td>
                <td>@flight.Aircraft?.Model</td>
            </tr>
        }
    </tbody>
</table>
